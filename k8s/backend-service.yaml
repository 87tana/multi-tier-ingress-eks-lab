apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: tannaz-2tier
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 3000       # service port
      targetPort: 3000 # container port in the pod
